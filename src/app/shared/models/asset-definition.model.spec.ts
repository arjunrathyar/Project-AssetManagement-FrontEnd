import { AssetDefinition.Model } from './asset-definition.model';

describe('AssetDefinition.Model', () => {
  it('should create an instance', () => {
    expect(new AssetDefinition.Model()).toBeTruthy();
  });
});
